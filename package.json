{
  "name": "sobu-dev",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "note": "bun scripts/note.ts",
    "cf:dev": "wrangler pages dev",
    "cf:deploy": "astro build && wrangler pages deploy",
    "cf:tail": "wrangler pages deployment tail --project-name=sobu-dev",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.8",
    "@astrojs/cloudflare": "^13.2.1",
    "@astrojs/mdx": "^5.0.4",
    "@astrojs/rss": "^4.0.18",
    "astro": "^6.1.9",
    "rehype-slug": "^6.0.0",
    "typescript": "^5.6.3",
    "unist-util-visit": "^5.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
